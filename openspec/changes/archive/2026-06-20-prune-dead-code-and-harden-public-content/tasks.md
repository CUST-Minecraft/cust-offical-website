## 1. 文档与范围确认

- [x] 1.1 阅读 `docs/产品需求文档.md`、`docs/总体设计文档.md`、`docs/详细设计文档.md` 和 `docs/开发规范文档.md` 中与维护态、公开 API、富文本渲染、外部服务入口和 mock fallback 相关的约束
- [x] 1.2 对照代码审查报告，确认只处理成立的问题：维护模式阻断、Activity 字段归一化、外部服务图标、富文本链接加固、重复代码和死代码清理
- [x] 1.3 确认公告 `featuredOnly`、成员精选去重、列表错误一律空状态等不成立或不纳入本次范围的审查项不会被实现
- [x] 1.4 实现前后检查 `docs/` 是否需要补充；如用户可见行为、接口语义或开发规范发生变化，同步更新对应文档

## 2. 公开内容维护态守卫

- [x] 2.1 新增 Nuxt server 侧公开内容维护守卫，读取 `maintenance-page` 并在维护模式开启时抛出 `MAINTENANCE`/503
- [x] 2.2 在生产环境中，当维护配置读取遇到 Strapi 不可用语义时返回维护态；开发环境继续允许既有开发 fallback
- [x] 2.3 将守卫接入首页、社团介绍、活动列表、活动详情、公告列表、公告详情、动态列表、动态详情、成员和加入页等内容型 `/api/public/*` 接口
- [x] 2.4 确认维护页、站点设置、外部服务和服务状态相关接口不接入内容阻断，仍可服务维护态页面渲染

## 3. CMS 输出归一化与服务入口

- [x] 3.1 调整 Activity 标准化逻辑，使 `endTime`、`location`、`signupUrl` 等可选字段只输出字符串或空值
- [x] 3.2 补齐外部服务图标允许列表，使 `agent` 成为有效公开服务图标
- [x] 3.3 确认未知图标仍回退到安全默认图标，且不会隐藏页脚或服务入口
- [x] 3.4 抽取重复的 populate 迁移错误判断 helper，保持 About/Join 等页面迁移兼容逻辑一致

## 4. 富文本与前端代码卫生

- [x] 4.1 加固富文本链接渲染：链接文本和 href 需要转义，仅允许 `http`/`https` 链接输出为 `<a>`
- [x] 4.2 将跨页面重复的日期展示逻辑抽取为共享格式化工具，并替换首页、活动、公告和动态页面中的本地重复函数
- [x] 4.3 删除无运行时作用的组件 refs、`void props` 占位和其他未引用局部代码
- [x] 4.4 删除不再被引用且不属于有效开发 fallback 的失效 mock 导出

## 5. 验证

- [x] 5.1 运行前端 TypeScript/Nuxt 类型检查，确认新增守卫、适配层和页面引用通过类型验证
- [x] 5.2 使用代码搜索确认旧重复日期函数、失效 mock 导出、无用 refs/props 占位和重复 populate helper 已清理
- [x] 5.3 使用代码搜索确认所有目标内容型 `/api/public/*` 接口已接入维护守卫，且维护态支撑接口未被误阻断
- [x] 5.4 复查本 change 的 specs、design 和 tasks，确认实现结果覆盖要求且没有引入本次明确排除的审查项

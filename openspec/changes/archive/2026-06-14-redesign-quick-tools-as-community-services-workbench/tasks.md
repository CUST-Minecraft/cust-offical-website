## 1. 文档与范围确认

- [x] 1.1 阅读 `docs/产品需求文档.md`、`docs/总体设计文档.md`、`docs/详细设计文档.md` 中全站搜索、快捷工具、文档中心、悦灵助手和服务状态相关段落。
- [x] 1.2 阅读 `openspec/specs/document-center-quick-tool/spec.md` 和已完成变更 `openspec/changes/redesign-quick-tools-as-workbench-panel/`，确认旧“快捷工具工作台”语义和本次“社团服务工作台”语义差异。
- [x] 1.3 更新 `docs/产品需求文档.md`，将用户可见“快捷工具”表述调整为“社团服务”，说明入口为工作台隐喻和“x 项服务可用”文案。
- [x] 1.4 更新 `docs/总体设计文档.md`，将快捷工具到文档中心、悦灵助手和服务状态的访问关系调整为社团服务工作台与面板级状态入口。
- [x] 1.5 更新 `docs/详细设计文档.md`，记录社团服务触发器、左侧服务列表、标题栏服务状态入口、悦灵离线态、呼吸高光和移动端布局规则。

## 2. 社团服务触发器

- [x] 2.1 重构 `frontend/components/FloatingServiceStatus.vue` 中右下角触发器的用户可见文案，将“快捷工具”替换为“社团服务”。
- [x] 2.2 将触发器副标题调整为“x 项服务可用”或等价文案，并确保 `x` 不计入面板级服务状态入口。
- [x] 2.3 新增或复用原创工作台方块风格图标资产，并在触发器中使用该图标作为社团服务整体隐喻。
- [x] 2.4 移除触发器中对书架、悦灵头像或其他具体服务特征的预览表达。
- [x] 2.5 在 `frontend/assets/css/main.css` 中为触发器添加未打开态轻微呼吸高光，并在面板打开、hover、focus 和 `prefers-reduced-motion: reduce` 下停止或弱化动画。

## 3. 工作台面板结构

- [x] 3.1 将面板标题、ARIA 标签、关闭按钮标题和相关可访问性文案从“快捷工具”调整为“社团服务”。
- [x] 3.2 调整工具数据结构命名和列表渲染语义，使左侧列表表示服务入口而不是泛用工具项。
- [x] 3.3 从左侧服务列表中移除“社团服务状态”或“服务状态”可选择项。
- [x] 3.4 保持左侧服务列表顺序为“文档中心 / 悦灵助手”，并继续区分外部服务与站内服务。
- [x] 3.5 保持右侧栏位根据当前服务切换内容，确保选择文档中心不会直接跳转外部页面。

## 4. 面板级服务状态

- [x] 4.1 在社团服务工作台标题栏右侧新增服务状态入口，展示在线计数、状态摘要或等价健康状态。
- [x] 4.2 确保服务状态入口与关闭按钮视觉和交互上清晰区分。
- [x] 4.3 为服务状态入口提供面板内服务状态详情，不再提供维护说明跳转入口。
- [x] 4.4 调整移动端标题栏布局，确保服务状态入口可读、可点按且不挤压关闭按钮。
- [x] 4.5 移除旧服务状态右侧栏位和左侧列表切换逻辑，避免服务状态仍表现为站内服务项。

## 5. 文档中心与悦灵助手行为

- [x] 5.1 保持文档中心从 `site.documentCenterUrl` 读取 URL，已配置时通过右侧“打开文档中心”按钮新标签页打开并使用 `noopener noreferrer`。
- [x] 5.2 保持文档中心未配置时左侧服务入口和右侧跳转按钮呈现禁用态，且不跳转到空 URL、当前页面、`/member` 或占位路由。
- [x] 5.3 保持文档中心书架方块图标只用于左侧服务入口和右侧文档中心内容，不用于社团服务触发器。
- [x] 5.4 调整悦灵助手离线态，移除“查看公告”作为主要 fallback 按钮。
- [x] 5.5 确保悦灵助手离线态只说明不可用原因，不展示维护说明 fallback 按钮。
- [x] 5.6 保持悦灵助手可用态的常用指令或对话入口，不从社团服务工作台再打开额外弹窗。

## 6. 视觉与响应式样式

- [x] 6.1 更新 `frontend/assets/css/main.css` 中 `.quick-*` 相关样式或逐步重命名为社团服务语义样式，确保用户可见样式与新命名一致。
- [x] 6.2 调整触发器尺寸、图标比例、像素边框、阴影和小字布局，使“社团服务 / x 项服务可用”在桌面端不溢出。
- [x] 6.3 调整标题栏布局，使标题、服务状态入口和关闭按钮在桌面端对齐稳定。
- [x] 6.4 调整移动端布局，使服务列表、右侧服务内容和标题栏状态入口不产生横向滚动或文本明显溢出。
- [x] 6.5 检查工作台图标、高光、状态入口和现有 Minecraft 像素风视觉的一致性，避免过度游戏化或强动画干扰阅读。

## 7. 验证

- [x] 7.1 运行前端类型检查或构建命令，确认社团服务工作台重构没有引入 TypeScript、Vue 或 Nuxt 构建错误。
- [x] 7.2 启动本地前端，验证右下角触发器展示工作台图标、“社团服务”和“x 项服务可用”。
- [x] 7.3 验证触发器未打开时存在轻微呼吸高光，打开面板后停止动画，`prefers-reduced-motion` 下停止或弱化动画。
- [x] 7.4 验证打开面板后标题为社团服务语境，左侧只展示“文档中心 / 悦灵助手”，不展示服务状态作为服务项。
- [x] 7.5 验证标题栏右侧服务状态入口显示正确计数或摘要，并能在工作台内打开完整服务状态面板。
- [x] 7.6 验证文档中心已配置和未配置两种状态下的右侧跳转按钮、安全打开策略和禁用态。
- [x] 7.7 验证悦灵助手在线、检测中和离线状态，确认离线态不再展示“查看公告”作为主要 fallback。
- [x] 7.8 使用桌面端和移动端视口检查面板布局、关闭方式、文本溢出、状态入口触控尺寸和按钮可读性。
- [x] 7.9 运行 OpenSpec 状态检查，确认 proposal、design、specs 和 tasks 均满足 apply 前置条件。

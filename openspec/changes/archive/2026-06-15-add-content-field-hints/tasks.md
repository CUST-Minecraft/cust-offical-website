## 1. 文档与范围确认

- [x] 1.1 阅读 `docs/详细设计文档.md`、`docs/总体设计文档.md` 和 `openspec/specs/content-model-boundaries/spec.md`，确认站点设置、外部服务、维护页和公开内容模型边界。
- [x] 1.2 检查现有 Header、Footer、首页、维护页和外部服务入口实现，列出第一阶段需要标注的结构化字段。
- [x] 1.3 确认字段提示入口的可见环境策略，避免普通生产访问者看到“字段”按钮。

## 2. 字段提示核心机制

- [x] 2.1 新增内容字段提示元信息类型，至少包含数据模型中文名、模型标识、字段名称和页面用途。
- [x] 2.2 建立字段提示元信息 registry 或等价集中管理方式，覆盖 `site-setting`、`external-service` 和 `maintenance-page` 的代表字段。
- [x] 2.3 实现字段提示状态管理，支持开启/关闭、hover active、click pinned 和关闭时清理状态。
- [x] 2.4 实现 `v-field-hint` 类似指令或等价机制，将字段元信息挂到原 DOM 元素而不新增影响布局的 wrapper。
- [x] 2.5 实现 body 级字段提示 overlay，根据目标元素位置绘制 outline 和气泡，并在 scroll/resize 时更新位置。

## 3. 页面字段接入

- [x] 3.1 在 `AppHeader.vue` 中为站点品牌、英文副标题、Logo 和皮肤站入口等可维护字段接入字段提示。
- [x] 3.2 在 `SiteFooter.vue` 中为站点品牌、版权、署名、关于我们链接和外部服务入口接入字段提示。
- [x] 3.3 在首页核心模块中为由 Strapi 结构化字段驱动的标题、摘要、按钮文案和外部服务入口接入字段提示。
- [x] 3.4 在维护页或服务状态相关展示中为维护页标题、说明和服务状态字段接入字段提示。
- [x] 3.5 确保字段提示不标注纯装饰素材、硬编码装饰文案或非维护人员可编辑内容。

## 4. Minecraft 风格视觉与交互

- [x] 4.1 在社团服务按钮上方实现固定“维护模式”按钮，使用 Minecraft 像素风钻石镐图标、石质/深板岩按钮视觉，并提供开启态反馈。
- [x] 4.2 为字段 outline 使用不影响布局的 `outline`、`box-shadow` 或 overlay rect，禁止使用改变尺寸的 `border`。
- [x] 4.3 为字段气泡实现像素风提示框样式，只显示数据模型、字段名称和页面用途。
- [x] 4.4 处理气泡靠近视口边缘时的定位，避免遮挡按钮、Header 关键操作和右下角快捷工具。
- [x] 4.5 确保搜索浮层或其他高优先级浮层打开时，字段提示层隐藏或退让。

## 5. 文档同步

- [x] 5.1 更新 `docs/详细设计文档.md`，记录内容字段提示层的触发方式、展示信息和不影响布局的约束。
- [x] 5.2 如 `docs/产品需求文档.md` 涉及内容维护验收流程，补充维护人员通过字段按钮识别数据模型和字段的说明。
- [x] 5.3 检查 `docs/开发规范文档.md` 是否需要补充字段提示不得使用内联可见文字或布局型 wrapper 的开发约束。

## 6. 验证

- [x] 6.1 运行前端类型检查，确认新增类型、指令和组件通过校验。
- [x] 6.2 在桌面视口验证默认页面无字段标注，点击社团服务按钮上方“维护模式”按钮后显示字段 outline。
- [ ] 6.3 验证 hover 字段时气泡只显示数据模型、字段名称和页面用途，不显示 API、响应 path 或组件技术细节。
- [x] 6.4 验证 click 字段后气泡固定，再次关闭字段模式后 outline、气泡和固定状态全部清除。
- [ ] 6.5 在移动或窄视口验证 Header、按钮、卡片和页脚布局不因字段提示发生换行、挤压或尺寸变化。

## 1. 文档与范围确认

- [x] 1.1 阅读 `docs/产品需求文档.md`、`docs/总体设计文档.md`、`docs/详细设计文档.md` 中全站搜索、快捷工具、外部服务边界和悦灵助手相关段落。
- [x] 1.2 更新 `docs/产品需求文档.md`，将快捷工具展示方式从右下角展开按钮列表调整为居中工作台面板，并说明外部工具与站内工具的区别。
- [x] 1.3 更新 `docs/总体设计文档.md`，调整快捷工具到文档中心外部系统的访问说明，明确官网仍不嵌入、不代理、不管理文档中心业务数据。
- [x] 1.4 更新 `docs/详细设计文档.md`，记录工作台面板的左侧工具列表、右侧工具栏位、站内工具轻量 UI、外部工具跳转按钮、禁用态和移动端布局。

## 2. 快捷工具组件结构

- [x] 2.1 重构 `frontend/components/FloatingServiceStatus.vue`，保留右下角快捷工具触发器，并移除原右下角纵向展开工具按钮列表。
- [x] 2.2 在快捷工具组件中新增居中工作台面板状态，支持打开、关闭、点击遮罩关闭和 Escape 关闭。
- [x] 2.3 建立工具列表数据结构，包含文档中心、悦灵助手、社团服务状态的 id、名称、图标、类型、状态标记和可用状态。
- [x] 2.4 实现左侧工具列表，列表项展示“图标 | 工具名称”，并保持顺序为“文档中心 / 悦灵助手 / 社团服务状态”。
- [x] 2.5 实现当前选中工具状态，选择左侧工具项时只切换右侧栏位内容，不立即触发外部跳转。

## 3. 右侧工具栏位

- [x] 3.1 实现文档中心外部工具栏位，展示书架图标、外部系统说明、配置状态和“打开文档中心”跳转按钮。
- [x] 3.2 保持 `documentCenterUrl` 已配置时新标签页打开，并使用 `noopener noreferrer` 或等价安全策略。
- [x] 3.3 实现 `documentCenterUrl` 未配置时的禁用态，确保左侧入口和右侧跳转按钮均不可触发跳转。
- [x] 3.4 实现悦灵助手站内工具栏位，展示轻量 UI，并至少提供状态、常用指令或对话入口中的一种可操作内容。
- [x] 3.5 实现社团服务状态站内工具栏位，展示在线数量、服务状态摘要和维护说明入口。
- [x] 3.6 移除旧悦灵助手与服务状态二级弹窗入口、组件和闲置样式，避免与右侧轻量 UI 出现冲突或重复弹窗。

## 4. 视觉与响应式样式

- [x] 4.1 在 `frontend/assets/css/main.css` 中新增快捷工具工作台遮罩、居中面板、左右栏布局和像素风边框样式。
- [x] 4.2 实现左侧工具列表的普通、选中、hover、禁用和外部/站内类型标识样式。
- [x] 4.3 实现右侧外部工具、悦灵助手、服务状态三类栏位样式，避免大面积彩色卡片堆叠。
- [x] 4.4 实现移动端布局，使工具列表和右侧栏位在小屏幕下不横向溢出，主要按钮保持可触控尺寸。
- [x] 4.5 检查文字长度、像素字体和按钮宽度，确保中文名称、状态值和禁用文案不会重叠或被截断。

## 5. 验证

- [x] 5.1 运行前端类型检查或构建命令，确认快捷工具重构没有引入 TypeScript、Vue 或 Nuxt 构建错误。
- [x] 5.2 启动本地前端，验证点击右下角触发器会打开居中工作台面板，而不是展开原纵向按钮列表。
- [x] 5.3 验证左侧工具列表顺序、图标、工具名称、选中态、外部/站内标识和服务状态计数。
- [x] 5.4 验证文档中心已配置 URL 时只通过右侧跳转按钮打开新标签页，并且选择左侧文档中心项不会立即跳转。
- [x] 5.5 验证文档中心未配置 URL 时展示禁用态，且不会跳转到空 URL、当前页面、`/member` 或占位路由。
- [x] 5.6 验证悦灵助手和社团服务状态能在右侧栏位直接展示轻量站内 UI。
- [x] 5.7 使用桌面端和移动端视口检查面板布局、遮罩、关闭方式、文本溢出和触控尺寸。
- [x] 5.8 运行 OpenSpec 校验或状态检查，确认 proposal、design、specs 和 tasks 均满足 apply 前置条件。

# Agent.md

## 项目基本说明

本项目是长春理工大学 Minecraft 社团官网，核心目标是建立社团统一的线上展示入口，并为公开访问者提供清晰的信息浏览和外部服务引导。

系统第一阶段重点建设：

- 官网前台：面向游客、校内学生、校外访问者和潜在新成员。
- 内容后台：第一阶段使用 Strapi Admin 管理官网内容。

皮肤站是独立系统，官网在 Header 提供公开外链入口，不嵌入、不代理、不管理皮肤站业务数据。资源包、整合包、地图和文件下载通过 QQ 群等社群渠道分发，不在官网建设下载服务。

自研后台不作为第一阶段必须建设内容。后续当活动报名、白名单审核、复杂成员系统、精细化权限等需求明确后，再作为独立系统规划和建设。

## 文档阅读顺序

开发或修改项目前，应先阅读下列文档。各文档职责不同，不应把同一类内容重复写入多份文档。

| 文档 | 作用 |
| --- | --- |
| [产品需求文档](./docs/产品需求文档.md) | 说明项目目标、用户角色、页面范围、功能边界和第一阶段范围 |
| [总体设计文档](./docs/总体设计文档.md) | 说明技术栈、基础设施、整体架构和第一阶段部署组成 |
| [详细设计文档](./docs/详细设计文档.md) | 说明页面设计、接口、数据模型、权限、缓存、SEO、媒体规范和非功能需求 |
| [自研后台设计文档](./docs/自研后台设计文档.md) | 说明后续自研后台的定位、技术栈、职责和迁移原则 |
| [开发规范文档](./docs/开发规范文档.md) | 说明代码注释、命名、TypeScript、Vue/Nuxt、CSS、接口调用、错误处理和 Git 规范 |

## 文档职责边界

- 产品需求文档只描述“要做什么”和“第一阶段做什么”。
- 总体设计文档只描述“系统怎么搭”和“技术怎么选”。
- 详细设计文档描述“页面、接口、模型、权限和规则如何落地”。
- 自研后台设计文档只描述后续自研后台，不替代第一阶段 Strapi 方案。
- 开发规范文档只描述“代码怎么写”和“协作怎么做”。

如果发现内容重复，应优先保留在最匹配的文档中，其他文档只保留引用。

## 技术方向

第一阶段技术选型以总体设计文档为准，核心方向如下：

- 前端：Nuxt 3、Vue 3、TypeScript。
- 样式：Tailwind CSS + 自定义 CSS，实现像素风视觉系统。
- 交互组件：Reka UI。
- 图标：Lucide Icons + 自定义 SVG。
- CMS：Strapi 5。
- 数据库：PostgreSQL。
- API 边界：前台通过 Nuxt Server Routes / Nitro 提供的 `/api/public/*` 访问公开数据。
- 媒体资源：对象存储 + CDN。
- 部署：Docker Compose + Nginx 或 Caddy。

## 开发约束

- 前端不得直接调用 Strapi 原始 API。
- 公开详情页使用 `slug`，不得在公开 URL 中暴露数据库 ID。
- 页面、接口、数据模型、缓存、SEO 和媒体规则以详细设计文档为准。
- 代码注释、命名、组件组织、接口调用封装和提交信息以开发规范文档为准。
- 修改页面范围、接口、数据模型、权限或技术栈时，必须同步更新对应文档。
- 不在公开页面展示真实姓名、学号、手机号等敏感信息。
- 不要把皮肤站业务逻辑写入官网项目。

## 当前目录说明

```text
docs/       项目文档与视觉资源
example/    当前静态原型，用于参考官网视觉方向和内容结构
CLAUDE.md   既有 Claude Code 使用说明
Agent.md    Agent 通用入口说明
```

`example/` 目录当前是静态原型，不代表最终 Nuxt 工程结构。后续正式开发时，应以设计文档和开发规范为准。

## Agent 工作原则

- 修改前先阅读相关文档，避免凭记忆改动。
- 如果需求、设计、规范之间出现冲突，应先指出冲突，再按用户最新确认的方向调整文档。
- 不要把详细设计内容写进产品需求文档。
- 不要把代码风格规范写进详细设计文档。
- 不要把后续自研后台内容混入第一阶段 Strapi 实施方案。
- 修改文档时保持中文命名和现有 Markdown 风格。

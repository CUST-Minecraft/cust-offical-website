# 长春理工大学 Minecraft 社团官网详细设计文档

## 1. 设计总则

### 1.1 页面优先原则

本详细设计按照前端页面划分板块。每个页面板块应说明：

- 页面路由与访问角色。
- 页面功能。
- 页面所需数据。
- 页面调用接口。
- 关联数据模型。
- 图片与媒体要求。
- SEO、缓存、权限和错误处理要求。

### 1.2 接口边界

- 官网前台统一访问 Nuxt Server Routes 提供的 `/api/public/*` 接口。
- 前台不直接调用 Strapi 原始 API。
- `/api/public/*` 面向游客，只返回已发布、可公开展示的数据。
- 详情页统一使用 `slug` 作为公开访问标识，不在 URL 中暴露数据库 ID。

### 1.3 通用返回结构

成功返回：

```json
{
  "success": true,
  "data": {},
  "meta": {}
}
```

分页返回：

```json
{
  "success": true,
  "data": [],
  "meta": {
    "page": 1,
    "pageSize": 10,
    "pageCount": 1,
    "total": 0
  }
}
```

失败返回：

```json
{
  "success": false,
  "error": {
    "code": "NOT_FOUND",
    "message": "内容不存在"
  }
}
```

### 1.4 通用错误码

| 状态码 | 业务码 | 说明 |
| --- | --- | --- |
| 400 | `BAD_REQUEST` | 请求参数错误 |
| 404 | `NOT_FOUND` | 内容不存在或未发布 |
| 429 | `RATE_LIMITED` | 请求过于频繁，后续可启用 |
| 500 | `INTERNAL_ERROR` | 服务内部错误 |
| 503 | `MAINTENANCE` | 维护模式或 CMS 不可用 |

### 1.5 通用分页参数

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | number | 否 | 页码，默认 1 |
| pageSize | number | 否 | 每页数量，默认 10 |
| category | string | 否 | 分类筛选 |
| tag | string | 否 | 标签筛选 |
| status | string | 否 | 状态筛选，主要用于活动 |
| keyword | string | 否 | 关键词搜索，可后续实现 |

### 1.6 当前已实现接口概览

本节记录当前仓库已经落地的 Nuxt Server Routes。接口返回结构仍遵循本文档“通用返回结构”。

公开内容接口：

| 接口 | 状态 | 说明 |
| --- | --- | --- |
| `GET /api/public/settings` | 已实现 | 返回站点信息、导航和服务状态 |
| `GET /api/public/home` | 已实现 | 返回首页聚合数据 |
| `GET /api/public/about` | 已实现 | 返回社团介绍页数据 |
| `GET /api/public/activities` | 已实现 | 支持 `page`、`pageSize`、`status` |
| `GET /api/public/activities/:slug` | 已实现 | 按 `slug` 返回活动详情 |
| `GET /api/public/announcements` | 已实现 | 支持 `page`、`pageSize`、`category` |
| `GET /api/public/announcements/:slug` | 已实现 | 按 `slug` 返回公告详情 |
| `GET /api/public/posts` | 已实现 | 支持 `page`、`pageSize`、`category`、`tag` |
| `GET /api/public/posts/:slug` | 已实现 | 按 `slug` 返回动态详情 |
| `GET /api/public/members` | 已实现 | 返回公开可见社员资料 |
| `GET /api/public/join` | 已实现 | 返回加入我们页数据 |
| `GET /api/public/maintenance` | 已实现 | 返回维护页和服务状态数据 |

可选外部服务接口：

| 接口 | 状态 | 说明 |
| --- | --- | --- |
| `GET /api/agent/status` | 部分实现 | 检查 `agentServiceUrl/health`，用于悦灵助手在线状态；真实对话能力待接入 |

### 1.7 公共数据模型

#### SiteSettings 站点配置

Single Type，全站唯一。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 站点完整名称 |
| shortName | string | 是 | 站点短名称 |
| englishName | string | 是 | 站点英文名或英文副标题 |
| logoText | string | 是 | 未配置 Logo 图片时显示的文字标识 |
| logoImage | media | 否 | 站点 Logo 图片 |
| favicon | media | 否 | 浏览器图标 |
| description | text | 否 | 站点描述 |
| copyright | text | 是 | 页脚版权信息 |
| credit | string | 是 | 页脚署名或致谢信息 |
| socials | json | 否 | 社交链接或外部账号数组 |
| skinConsoleUrl | string | 是 | Header 皮肤站按钮外部跳转地址 |
| documentCenterUrl | string | 否 | 快捷工具工作台中文档中心外部跳转地址 |
| navigation | component[] | 是 | 主导航链接 |
| serviceStatusLabel | string | 是 | 服务状态入口标题 |
| serviceStatusHref | string | 是 | 服务状态详情页地址，默认 `/maintenance` |
| serviceStatusServices | component[] | 否 | 服务状态条目 |
| maintenanceEnabled | boolean | 是 | 是否开启维护模式 |
| maintenanceTitle | string | 否 | 维护页标题 |
| maintenanceMessage | text | 否 | 维护页说明 |
| maintenanceUntil | datetime | 否 | 预计恢复时间 |

#### Tag 标签

Collection Type，可多条。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 标签名称 |
| slug | uid | 是 | URL 标识 |
| color | string | 否 | 标签颜色 |

#### 通用组件

| 组件 | 用途 |
| --- | --- |
| NavLink | 导航链接 |
| HeroSlide | 首页 Hero 轮播图配置 |
| TextItem | 可重复短文本条目 |
| ContactMethod | 联系方式 |
| FaqItem | 常见问题 |
| ProcessStep | 流程步骤 |
| GroupInfo | 社团分组 |
| PromoSection | 首页简介或推广区块 |
| ServerInfo | 服务器概览 |
| AttachmentLink | 公告附件链接 |
| ServiceStatusItem | 服务状态条目 |

## 2. 公开页面详细设计

### 2.1 首页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 建立第一印象，聚合官网核心入口和最新内容 |
| 主要接口 | `GET /api/public/home` |
| 缓存策略 | CDN 短缓存，建议 1 到 5 分钟 |

功能说明：

- 展示社团品牌标识、Hero 主视觉和社团简介。
- 展示最新公告、近期活动、最新社团动态。
- 展示服务器概览、作品展示、社员介绍入口、加入入口和 Header 皮肤站入口。
- 首页内容通过聚合接口返回，减少首屏请求数量。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 站点名称、Logo、导航、页脚 | SiteSettings | 全站公共信息 |
| Hero 标题、副标题、主视觉 | HomePage | 首页首屏内容 |
| 社团简介 | HomePage | 首页简介模块 |
| 最新公告 3 条 | Announcement | 按置顶和发布时间展示 |
| 近期活动 3 条 | Activity | 按活动时间和状态展示 |
| 最新动态 4 条 | ClubPost | 按发布时间展示 |
| 精选社员 | MemberProfile | 首页精选展示 |
| 作品展示 | GalleryItem | 首页精彩瞬间 |

HomePage 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| heroEyebrow | string | 是 | Hero 眉标文案 |
| heroTitleLines | component[] | 是 | Hero 主标题多行文本 |
| heroHighlightText | string | 是 | Hero 高亮文案 |
| heroSubtitle | string | 是 | Hero 副标题 |
| heroDescription | text | 是 | Hero 描述 |
| heroSlides | component[] | 是 | Hero 轮播背景与文案样式配置 |
| carouselInterval | integer | 是 | Hero 轮播间隔，默认 6000ms |
| intro | component | 是 | 首页简介或推广区块 |
| server | component | 是 | 首页服务器概览 |
| featuredActivities | relation[] | 否 | 首页精选活动 |
| featuredAnnouncements | relation[] | 否 | 首页精选公告 |
| featuredPosts | relation[] | 否 | 首页精选动态 |
| featuredMembers | relation[] | 否 | 首页精选社员 |
| galleryItems | relation[] | 否 | 首页作品或精彩瞬间 |

当前接口说明：

- `GET /api/public/home` 当前会读取 `home-page` 的 Hero、简介和服务器概览配置。
- 首页活动、公告、动态、社员和图库展示当前由聚合接口按推荐或排序规则拉取，不完全依赖 `home-page` 的精选关系。
- Hero 轮播图使用 `HeroSlide` 组件配置背景图、文字位置、遮罩强度、文字宽度、明暗色调和背景焦点位置。

媒体要求：

- Hero 背景图建议 1920x900，格式 WebP 或 PNG。
- 首页卡片封面统一按 16:9 展示。
- 非首屏图片可懒加载。

SEO 要求：

- 使用站点标题、站点描述和首页分享图。
- 首页应包含主要关键词：社团名称、学校名称、Minecraft 社团等。

错误处理：

- CMS 不可用时优先使用缓存。
- 无可用缓存时展示维护提示页。

### 2.2 社团介绍页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/about` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示社团背景、社团方向、社团文化、分组和服务器概览 |
| 主要接口 | `GET /api/public/about` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示社团介绍正文。
- 展示社团文化、口号和发展方向。
- 展示主要分组，例如建筑组、红石组、活动组、运维组。
- 展示服务器概览和加入引导。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 页面标题、摘要、头图 | AboutPage | 页面基础内容 |
| 社团介绍正文 | AboutPage | 主要正文 |
| 社团分组 | AboutPage.groups | 结构化分组信息 |
| 服务器概览 | AboutPage.serverSummary | 可后续拆分为服务器页 |
| 加入引导 | AboutPage.joinGuide | 指向 `/join` |

AboutPage 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 是 | 页面标题 |
| summary | text | 否 | 页面摘要 |
| coverImage | media | 否 | 页面头图 |
| content | richtext | 是 | 社团介绍正文 |
| groups | component[] | 否 | 社团分组 |
| serverSummary | richtext | 否 | 服务器概览 |
| joinGuide | richtext | 否 | 加入引导 |

媒体要求：

- 页面头图建议 1920x900。
- 分组图标可使用 Lucide Icons、自定义 SVG 或像素风 PNG。

SEO 要求：

- 页面标题应包含社团名称。
- `description` 使用 `summary` 字段。

错误处理：

- 页面数据不存在时展示 404。
- CMS 不可用且无缓存时展示维护提示。

### 2.3 社团活动页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/activities` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示活动列表，支持浏览近期活动和历史活动 |
| 主要接口 | `GET /api/public/activities` |
| 缓存策略 | CDN 短缓存，建议 1 到 5 分钟 |

功能说明：

- 展示活动列表。
- 支持分页。
- 支持按活动状态筛选。
- 支持按分类或标签筛选，标签能力可后续启用。
- 活动列表按活动时间倒序或活动状态排序。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| page | number | 否 | 页码 |
| pageSize | number | 否 | 每页数量 |
| status | string | 否 | upcoming、ongoing、ended、cancelled |
| tag | string | 否 | 标签筛选，可后续启用 |

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 活动标题 | Activity.title | 列表卡片标题 |
| 活动摘要 | Activity.summary | 列表摘要 |
| 活动封面 | Activity.coverImage | 列表封面 |
| 活动时间 | Activity.startTime、Activity.endTime | 展示时间范围 |
| 活动地点 | Activity.location | 可为空 |
| 活动状态 | Activity.status | 展示状态标识 |
| 分页信息 | API meta | 页码、总数 |

Activity 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 是 | 活动标题 |
| slug | uid | 是 | URL 标识 |
| summary | text | 否 | 活动简介 |
| content | richtext | 是 | 活动详情 |
| coverImage | media | 是 | 活动封面 |
| gallery | media[] | 否 | 活动图集 |
| startTime | datetime | 是 | 开始时间 |
| endTime | datetime | 否 | 结束时间 |
| location | string | 否 | 活动地点 |
| status | enum | 是 | upcoming、ongoing、ended、cancelled |
| signupUrl | string | 否 | 报名外链 |
| isFeatured | boolean | 是 | 是否推荐 |
| publishedAt | datetime | 是 | 发布时间 |

媒体要求：

- 活动封面建议 1200x675，16:9，格式 WebP 或 JPEG。

错误处理：

- 无活动时展示空状态。
- 分页超出范围时展示空列表或返回第一页。

### 2.4 活动详情页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/activities/:slug` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示单个活动完整信息 |
| 主要接口 | `GET /api/public/activities/:slug` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示活动标题、正文、封面、图集、时间、地点和状态。
- 支持展示报名链接。
- 第一阶段报名使用外部链接，不在官网内实现报名流程。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 活动标题、摘要、正文 | Activity | 主要内容 |
| 封面和图集 | Activity.coverImage、Activity.gallery | 图片展示 |
| 活动时间和地点 | Activity | 时间地点信息 |
| 活动状态 | Activity.status | 状态展示 |
| 报名链接 | Activity.signupUrl | 外部报名入口 |

SEO 要求：

- `title` 使用活动标题。
- `description` 使用活动摘要。
- Open Graph 图片使用活动封面。

错误处理：

- `slug` 不存在、内容未发布或已归档时展示 404。
- 报名链接为空时不展示报名按钮。

### 2.5 社团公告页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/announcements` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示正式通知列表 |
| 主要接口 | `GET /api/public/announcements` |
| 缓存策略 | CDN 短缓存，建议 1 到 5 分钟 |

功能说明：

- 公告用于正式通知，与社团动态区分。
- 支持置顶公告。
- 支持分页和分类筛选。
- 列表优先展示置顶内容，再按发布时间倒序展示。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 公告标题 | Announcement.title | 列表标题 |
| 公告摘要 | Announcement.summary | 列表摘要 |
| 公告分类 | Announcement.category | 分类展示 |
| 发布时间 | Announcement.publishedAt | 时间展示 |
| 置顶状态 | Announcement.isPinned | 置顶展示 |

Announcement 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 是 | 公告标题 |
| slug | uid | 是 | URL 标识 |
| category | enum/string | 否 | 公告分类 |
| summary | text | 否 | 公告摘要 |
| content | richtext | 是 | 公告正文 |
| attachments | media[] | 否 | 附件 |
| isPinned | boolean | 是 | 是否置顶 |
| publishedAt | datetime | 是 | 发布时间 |

错误处理：

- 无公告时展示空状态。
- 分类无结果时展示空状态并保留清除筛选入口。

### 2.6 公告详情页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/announcements/:slug` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示单条公告详情 |
| 主要接口 | `GET /api/public/announcements/:slug` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示公告标题、分类、发布时间、正文和附件。
- 附件可通过对象存储和 CDN 访问。

SEO 要求：

- `title` 使用公告标题。
- `description` 使用公告摘要或正文截取。

错误处理：

- `slug` 不存在、内容未发布或已归档时展示 404。
- 附件加载失败时展示附件不可用提示。

### 2.7 社团动态页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/posts` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示社团博文、日常记录、作品内容和服务器进展 |
| 主要接口 | `GET /api/public/posts` |
| 缓存策略 | CDN 短缓存，建议 1 到 5 分钟 |

功能说明：

- 展示社团动态列表。
- 支持分页、分类筛选和标签筛选。
- 支持推荐动态，用于首页或列表页重点展示。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 动态标题 | ClubPost.title | 列表标题 |
| 动态摘要 | ClubPost.summary | 列表摘要 |
| 动态封面 | ClubPost.coverImage | 卡片封面 |
| 作者 | ClubPost.authorName | 展示作者 |
| 分类和标签 | ClubPost.category、ClubPost.tags | 筛选和展示 |
| 发布时间 | ClubPost.publishedAt | 时间展示 |

ClubPost 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 是 | 动态标题 |
| slug | uid | 是 | URL 标识 |
| summary | text | 否 | 摘要 |
| content | richtext | 是 | 正文 |
| coverImage | media | 否 | 封面图 |
| gallery | media[] | 否 | 图集 |
| authorName | string | 否 | 展示作者 |
| category | string | 否 | 分类 |
| tags | relation[] | 否 | 标签 |
| isFeatured | boolean | 是 | 是否推荐 |
| publishedAt | datetime | 是 | 发布时间 |

媒体要求：

- 动态封面建议 1200x675，16:9，格式 WebP 或 JPEG。

错误处理：

- 无动态时展示空状态。
- 标签不存在时展示 404 或空列表，第一阶段建议空列表。

### 2.8 社团动态详情页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/posts/:slug` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示单篇社团动态详情 |
| 主要接口 | `GET /api/public/posts/:slug` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示动态标题、正文、封面、图集、作者、分类、标签和发布时间。
- 正文支持富文本或 Markdown 渲染。

SEO 要求：

- `title` 使用动态标题。
- `description` 使用动态摘要或正文截取。
- Open Graph 图片使用动态封面。

错误处理：

- `slug` 不存在、内容未发布或已归档时展示 404。
- 正文图片加载失败时使用默认占位图。

### 2.9 社员介绍页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/members` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 展示公开社员资料、分组和贡献 |
| 主要接口 | `GET /api/public/members` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示社员公开资料。
- 支持按分组展示。
- 可展示头像、Minecraft 皮肤形象、简介、职责和代表作品。
- 不展示真实姓名、学号、手机号等敏感信息。
- 社员公开资料默认由内容管理员维护。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 展示名 | MemberProfile.displayName | 公开展示名称 |
| 头像或皮肤形象 | MemberProfile.avatar、MemberProfile.skinImage | 公开形象 |
| 所属分组 | MemberProfile.group | 分组展示 |
| 社内职责 | MemberProfile.roleTitle | 职责或称号 |
| 简介 | MemberProfile.bio | 简短介绍 |
| 代表作品 | MemberProfile.works | 关联作品 |

MemberProfile 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| displayName | string | 是 | 展示名 |
| slug | uid | 是 | URL 标识 |
| avatar | media | 否 | 头像 |
| skinImage | media | 否 | Minecraft 皮肤形象 |
| group | string | 否 | 所属分组 |
| roleTitle | string | 否 | 社内称号或职责 |
| bio | text | 否 | 简介 |
| works | relation[] | 否 | 代表作品 |
| sortOrder | integer | 是 | 排序 |
| isVisible | boolean | 是 | 是否公开展示 |

媒体要求：

- 社员头像建议 512x512，1:1，格式 WebP 或 PNG。
- 皮肤形象建议 512x512，格式 PNG 或 WebP。

错误处理：

- 无公开社员资料时展示空状态。
- 不可见社员资料不进入公开接口。

### 2.10 加入我们页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/join` |
| 访问角色 | 游客、社员、内容管理员、超级管理员 |
| 页面目标 | 承接新成员转化，展示加入方式和入社流程 |
| 主要接口 | `GET /api/public/join` |
| 缓存策略 | CDN 中缓存，建议 10 到 30 分钟 |

功能说明：

- 展示社团加入方式、入社流程、联系方式和常见问题。
- 展示服务器加入条件、社团群入口和注意事项。
- 支持配置多个联系方式，例如 QQ 群、邮箱、表单链接等。
- 支持外部报名表或问卷链接。
- 第一阶段不在官网内实现完整报名审核流程。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 页面标题、摘要、头图 | JoinPage | 页面基础信息 |
| 加入说明 | JoinPage.introContent | 主体介绍 |
| 加入条件 | JoinPage.requirements | 条件列表 |
| 入社流程 | JoinPage.processSteps | 步骤列表 |
| 联系方式 | JoinPage.contactMethods | QQ 群、邮箱等 |
| 常见问题 | JoinPage.faqItems | FAQ |
| 外部报名链接 | JoinPage.applicationUrl | 外部问卷或表单 |
| 服务器加入说明 | JoinPage.serverJoinGuide | 服务器准入信息 |

JoinPage 数据模型：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| title | string | 是 | 页面标题 |
| summary | text | 否 | 页面摘要 |
| coverImage | media | 否 | 页面头图 |
| introContent | richtext | 是 | 加入说明 |
| requirements | component[] | 否 | 加入条件 |
| processSteps | component[] | 否 | 入社流程 |
| contactMethods | component[] | 是 | 联系方式 |
| faqItems | component[] | 否 | 常见问题 |
| applicationUrl | string | 否 | 外部报名表或问卷链接 |
| serverJoinGuide | richtext | 否 | 服务器加入说明 |

SEO 要求：

- `title` 使用加入我们页标题。
- `description` 使用加入说明摘要。

错误处理：

- 外部报名链接为空时不展示报名按钮。
- 联系方式为空时展示站点公共联系方式。

### 2.11 Header 皮肤站按钮

| 项目 | 内容 |
| --- | --- |
| 展示位置 | 全站 Header |
| 访问角色 | 所有访问者 |
| 页面目标 | 提供皮肤站独立系统的公开外链入口 |
| 数据来源 | SiteSettings.skinConsoleUrl |
| 缓存策略 | 随站点配置短缓存 |

功能说明：

- Header 原登录按钮位置展示“皮肤站”按钮。
- 按钮继续使用地狱门图标，文案为“皮肤站”。
- 点击按钮后直接跳转到 `skinConsoleUrl`。
- 官网不校验皮肤站访问权限，不代理、不嵌入、不管理皮肤站业务数据。

页面数据：

| 数据 | 来源模型 | 说明 |
| --- | --- | --- |
| 皮肤站地址 | SiteSettings.skinConsoleUrl | 外部系统 URL |
| 地狱门图标 | 静态素材 | 复用原 Header 登录按钮图标 |

错误处理：

- `skinConsoleUrl` 未配置时不展示按钮或展示不可用状态。
- 外部系统不可用由皮肤站自身处理，官网不提供中转页。

### 2.12 全站搜索与快捷工具

| 项目 | 内容 |
| --- | --- |
| 展示位置 | 全站布局 |
| 访问角色 | 所有访问者 |
| 当前状态 | 已完成前端交互，搜索数据来自已加载的公开接口 |

全站搜索：

- Header 中提供搜索入口，打开后展示搜索浮层。
- 搜索范围包括导航入口、皮肤站入口、文档中心入口、服务状态入口、活动、公告、动态和公开社员资料。
- 当前搜索在前端基于已请求的公开数据构建索引，主要用于第一阶段站内快速跳转。
- 搜索浮层应支持关闭、键盘聚焦、回车进入首个结果和空状态提示。

快捷工具：

- 页面右下方展示快捷工具入口，点击后打开居中的快捷工具工作台面板。
- 快捷工具工作台左侧展示工具列表，列表项采用“图标 | 工具名称”结构，顺序为“文档中心 / 悦灵助手 / 社团服务状态”。
- 快捷工具工作台右侧展示当前选中工具的栏位内容：站内工具直接在右侧栏位展示 UI，不再跳转或打开额外弹窗；外部工具展示说明和跳转按钮。
- 文档中心属于外部工具；悦灵助手和社团服务状态属于站内工具，应通过分组、标记或状态值清楚区分。
- 文档中心数据来自 `GET /api/public/settings` 中的 `site.documentCenterUrl`。
- `documentCenterUrl` 已配置时，文档中心右侧跳转按钮以新标签页打开外部文档中心地址，并使用 `noopener noreferrer`。
- `documentCenterUrl` 未配置时，文档中心左侧入口和右侧跳转按钮仍展示，但以禁用态呈现且不可跳转。
- 服务状态数据来自 `GET /api/public/settings` 中的 `serviceStatus`。
- 服务状态右侧栏位展示在线数量、服务状态摘要和维护说明入口；完整维护说明仍由维护页承载，不再通过额外弹窗重复展示服务状态。
- 移动端快捷工具工作台应调整为适配视口的上下布局，避免工具列表和右侧栏位横向溢出。

悦灵助手：

- 当前已有前端入口、常用指令、在线状态检测和离线 fallback；快捷工具工作台右侧栏位展示适合快捷场景的 UI，并在 Agent 服务未配置或不可用时直接展示不可用提示和 fallback 操作，不再从快捷工具打开额外 Agent 弹窗。
- 在线状态通过 `GET /api/agent/status` 检查 `agentServiceUrl/health`。
- 第一阶段真实 Agent 对话服务尚未接入时，应展示明确离线或占位提示，不应伪装为已完成智能问答。

## 3. 外部服务边界

### 3.1 皮肤站

| 项目 | 内容 |
| --- | --- |
| 访问方式 | Header “皮肤站”外链按钮 |
| 访问角色 | 所有访问者 |
| 官网职责 | 展示入口并跳转到 `skinConsoleUrl` |
| 外部职责 | 皮肤站自行处理账号、登录和访问控制 |

功能说明：

- 皮肤站是独立系统。
- 官网不嵌入、不代理、不管理皮肤站业务数据。
- 官网不校验访问者是否为社员。
- Header 按钮使用地狱门图标，点击后直接访问配置的皮肤站网址。

### 3.2 CMS 内容后台

| 项目 | 内容 |
| --- | --- |
| 访问方式 | 管理员直接访问 Strapi Admin 或后续后台系统 |
| 访问角色 | CMS 管理员 |
| 官网职责 | 不展示公开后台入口，不负责后台权限判断 |
| 外部职责 | CMS 自身处理账号、登录、权限和内容管理 |

功能说明：

- 第一阶段由 Strapi Admin 承担内容管理。
- 内容管理员不通过官网跳转到 CMS。
- 官网公开接口只返回已发布、可公开展示的数据。

### 3.3 文档中心

| 项目 | 内容 |
| --- | --- |
| 访问方式 | 快捷工具工作台“文档中心”外部工具入口 |
| 访问角色 | 所有访问者 |
| 官网职责 | 展示入口并跳转到 `documentCenterUrl`；未配置地址时展示禁用态 |
| 外部职责 | 文档中心自行处理账号、权限、文档编辑和访问控制 |

功能说明：

- 文档中心是独立外部系统。
- 官网不嵌入、不代理、不同步、不管理文档中心业务数据。
- 官网不校验访问者是否为社员。
- 快捷工具工作台中的文档中心入口使用书架方块风格 SVG 图标，展示在“悦灵助手”上方。
- 访问者选择文档中心工具时，官网在右侧栏位展示外部系统说明和跳转按钮；选择左侧入口本身不直接跳转。
- 文档中心不属于服务状态条目，不写入 `serviceStatus.services`。
- 文档中心不承担官网资源包、整合包、地图或文件下载分发能力。

## 4. 错误与维护页面详细设计

### 4.1 404 页面

| 项目 | 内容 |
| --- | --- |
| 路由 | 自动匹配 |
| 访问角色 | 所有用户 |
| 页面目标 | 处理不存在的页面或内容 |
| 数据来源 | SiteSettings |
| 缓存策略 | 可短缓存 |

触发场景：

- 路由不存在。
- 详情内容不存在。
- `slug` 错误。
- 内容未发布或已归档。

功能说明：

- 展示错误提示。
- 提供返回首页入口。
- 可提供常用页面入口，例如活动、公告、动态、加入我们。

SEO 要求：

- 不建议被索引。
- 返回正确的 404 状态码。

### 4.2 维护提示页

| 项目 | 内容 |
| --- | --- |
| 路由 | `/maintenance` |
| 访问角色 | 游客、社员 |
| 页面目标 | 官网维护或服务不可用时展示 |
| 数据来源 | SiteSettings，Strapi 不可用时使用内置最小兜底内容 |
| 缓存策略 | 不缓存或极短缓存 |

触发场景：

- `SiteSettings.maintenanceEnabled` 为 true。
- CMS 不可用且没有可用缓存。
- 管理员主动开启维护模式。

错误语义：

- CMS 连接失败、超时或无响应时，应返回 `MAINTENANCE`/503，不应误判为 404。
- Strapi 正常响应但目标内容不存在、未发布或已归档时，才返回 `NOT_FOUND`/404。
- 生产环境不得使用 mock 数据作为维护态或公开内容兜底。
- 维护页必须能在 SiteSettings 不可用时使用内置站点名称、维护标题和维护说明渲染。

页面数据：

| 数据 | 来源字段 | 说明 |
| --- | --- | --- |
| 维护标题 | SiteSettings.maintenanceTitle | 维护页标题 |
| 维护说明 | SiteSettings.maintenanceMessage | 维护原因或说明 |
| 预计恢复时间 | SiteSettings.maintenanceUntil | 可为空 |
| 联系方式 | SiteSettings.contactMethods | 可选 |

SEO 要求：

- 不建议长期被索引。
- 可返回 503 状态码。

## 5. 可选独立页面

### 5.1 服务器信息页

| 项目 | 内容 |
| --- | --- |
| 建议路由 | `/server` |
| 第一阶段处理 | 可并入首页或社团介绍页 |
| 页面目标 | 展示服务器地址、版本、玩法、规则和状态 |

说明：

- 第一阶段服务器状态可由管理员手动维护。
- 后续可接入真实服务器状态查询。

### 5.2 下载服务

| 项目 | 内容 |
| --- | --- |
| 官网处理 | 不建设资源下载页 |
| 分发方式 | 通过 QQ 群等社群渠道分发资源包、整合包、地图和文件 |

说明：

- 官网不提供 `/resources` 页面。
- 官网不提供公开资源下载 API。
- Strapi 不维护官网资源下载内容模型。

## 6. 媒体与附件规范

| 类型 | 用途 | 建议尺寸 | 格式 |
| --- | --- | --- | --- |
| Hero 背景图 | 首页和重要页面头图 | 1920x900 | WebP、PNG |
| 活动封面 | 活动列表和详情页 | 1200x675 | WebP、JPEG |
| 动态封面 | 社团动态列表和详情页 | 1200x675 | WebP、JPEG |
| 社员头像 | 社员介绍页 | 512x512 | WebP、PNG |
| 皮肤形象 | 社员 Minecraft 形象展示 | 512x512 | PNG、WebP |
| 图集图片 | 活动、动态、作品展示 | 最大宽度 1600 | WebP、JPEG |
| 附件 | 公告附件 | 按文件类型 | PDF、ZIP、7Z 等 |
| 像素风装饰素材 | 图标、边框、角标、地形块 | 按展示需要 | SVG、PNG |

上传与压缩规则：

- 普通图片优先使用 WebP。
- 需要透明背景的素材使用 PNG 或 SVG。
- 正文图片单张建议不超过 2 MB。
- Hero 图片单张建议不超过 4 MB。
- 附件第一阶段建议单文件不超过 50 MB。
- 上传文件应使用英文、数字和短横线命名，避免空格和特殊符号。
- 图片、附件等媒体资源统一由对象存储承载，并通过 CDN 访问。

裁剪规则：

- 列表封面统一按 16:9 展示。
- 社员头像统一按 1:1 展示。
- Hero 图片允许响应式裁剪，主体内容应尽量位于画面中心区域。
- 正文图片不强制裁剪，但前端应限制最大显示宽度。

## 7. 内容发布流程

### 7.1 内容管理边界

- 官网前台面向所有访问者展示公开内容。
- 内容管理员通过 Strapi Admin 或后续后台系统管理活动、公告、动态、社员介绍、加入我们页等公开内容。
- CMS 管理账号和权限由 CMS 或后续后台系统自身承担。
- 公开页面只展示已发布、未隐藏、可公开访问的内容。
- 社员公开资料默认由管理员维护，普通社员后续可考虑申请修改。

### 7.2 内容状态

| 状态 | 说明 |
| --- | --- |
| draft | 草稿，前台不可见 |
| published | 已发布，前台可见 |
| archived | 已归档，前台默认不可见 |

操作规则：

- 内容管理员和超级管理员可以创建草稿。
- 内容管理员和超级管理员可以发布、编辑、归档内容。
- 删除操作应谨慎使用，优先使用归档代替删除。
- 置顶和推荐只对已发布内容生效。
- 修改已发布内容后，应触发 CDN 缓存刷新或等待短缓存自然过期。
- 上传图片应填写替代文本或说明，便于无障碍访问和内容维护。
- 社员介绍、公告、动态等公开内容不得包含真实姓名、学号、手机号等敏感信息。

审核规则：

- 第一阶段可不实现复杂审核流，由内容管理员直接发布。
- 后续自研后台可增加提交、审核、退回、发布等完整流程。

## 8. 缓存与 SEO 总体规则

### 8.1 缓存规则

| 内容 | 缓存策略 |
| --- | --- |
| 首页 | CDN 短缓存，建议 1 到 5 分钟 |
| 列表接口 | CDN 短缓存，建议 1 到 5 分钟 |
| 详情接口 | CDN 中缓存，建议 10 到 30 分钟 |
| 站点配置 | CDN 短缓存，维护模式开启时不缓存或极短缓存 |
| 图片资源 | CDN 长缓存，建议 7 到 30 天 |
| 附件资源 | CDN 长缓存，按文件更新频率调整 |
| Agent 状态接口 | 不使用 CDN 缓存或仅极短缓存 |
| 维护态响应 | 不缓存或极短缓存 |

缓存中间件选型以总体设计文档为准；本节只定义页面与接口层面的缓存策略。第一阶段保留 CDN 作为主要缓存层，暂不引入 Redis、KV 或服务端持久 last-known-good 缓存；当 CDN 无可用缓存且 Strapi 不可用时，公开页面和公开 API 使用维护态响应。

### 8.2 SEO 规则

- 自动生成 `title`、`description`、Open Graph 信息。
- 公开详情页使用 canonical URL。
- 生成 `sitemap.xml`，包含公开页面和已发布详情页。
- 配置 `robots.txt`，禁止索引维护页等非公开内容页面。
- 详情页摘要优先使用 `summary` 字段，没有摘要时从正文中截取。
- 404 页面不应被索引。
- 维护页不应长期被索引。

## 9. Strapi 内容类型映射

### 9.1 Single Type

| 模型 | Strapi 类型 |
| --- | --- |
| SiteSettings | Single Type |
| HomePage | Single Type |
| AboutPage | Single Type |
| JoinPage | Single Type |

### 9.2 Collection Type

| 模型 | Strapi 类型 |
| --- | --- |
| Activity | Collection Type |
| Announcement | Collection Type |
| ClubPost | Collection Type |
| MemberProfile | Collection Type |
| GalleryItem | Collection Type |
| Tag | Collection Type |

### 9.3 字段类型映射

| 文档类型 | Strapi 字段类型 |
| --- | --- |
| string | Text、Short Text |
| text | Long Text |
| richtext | Rich Text 或 Blocks |
| uid | UID |
| enum | Enumeration |
| boolean | Boolean |
| integer | Number |
| datetime | DateTime |
| media | Media |
| relation | Relation |
| component | Component |

## 10. 非功能需求

### 10.1 页面实现约束

- 页面路由、接口路径、数据模型字段应优先遵循本文档。
- 页面级数据获取应集中在页面或页面级 composable 中，避免展示组件直接分散请求接口。
- 所有公开列表页必须处理加载中、空状态、错误状态和分页状态。
- 所有公开详情页必须处理 404。
- 站点配置和皮肤站地址应从配置读取，不在多个页面硬编码。
- 不允许将可由 CMS 维护的核心内容长期硬编码在前端页面中。

### 10.2 UI 与视觉约束

- 官网前台采用自定义像素风视觉，但不应牺牲可读性和可用性。
- 像素风主要通过 CSS、图片素材和 SVG 装饰实现，不把正文内容做成图片。
- 卡片、按钮、导航、列表、标签等基础组件应保持统一视觉风格。
- 文本不得溢出按钮、卡片、导航栏或图片容器。
- 页面在移动端不得出现非预期横向滚动。
- 按钮、导航、链接等交互元素应有明确的 hover、active、disabled 状态。
- 图标优先使用 Lucide Icons；像素风特殊图标可使用自定义 SVG 或 PNG。

### 10.3 响应式与兼容性

- 支持桌面端、平板和移动端访问。
- 移动端优先保证导航、列表、详情正文和皮肤站入口可用。
- 页面文本不得与按钮、图片、卡片发生重叠。

### 10.4 性能要求

- 首页应通过聚合接口减少请求数量。
- 全站搜索当前为前端索引，不应额外引入未规划的公开搜索 API；若后续接入服务端搜索，应同步更新接口设计和缓存策略。
- 图片应使用压缩格式，并按展示区域加载合适尺寸。
- 首屏关键内容应优先加载，非关键图片可懒加载。
- 公开 API 应配置合理的 Cache-Control。

### 10.5 安全要求

- 官网不提供账号注册、登录、会话或社员角色权限服务。
- 外部链接应明确跳转，不在页面中静默嵌入高权限系统。
- Agent 服务地址、健康检查和后续对话接口必须通过服务端配置访问，不应在前端硬编码敏感地址或密钥。
- 富文本内容需要进行 XSS 防护或使用可信渲染方案。
- 不在公开页面展示真实姓名、学号、手机号等敏感信息。
- 公开接口不得返回后台管理字段、草稿内容、隐藏内容、权限字段和不必要的内部 ID。
- 生产环境不得使用默认密钥、默认管理员账号或测试密码。

### 10.6 CMS 内容约束

- Strapi 内容类型应与本文档中的模型保持一致。
- 媒体资源统一通过对象存储保存，并通过 CDN 访问。
- 内容字段调整后，应同步检查前台接口返回结构和页面展示。

### 10.7 备份与日志

- PostgreSQL 应定期备份。
- 对象存储中的媒体文件应保留备份策略。
- 日志方案以总体设计文档为准。
- 后续可根据运维需要增加接口访问日志、错误日志和内容操作审计日志。

### 10.8 可访问性

- 导航、按钮、链接应可通过键盘访问。
- 图片应提供合理的替代文本。
- 颜色对比度应满足基本阅读要求。
- 交互组件应使用可访问性良好的基础组件实现。
